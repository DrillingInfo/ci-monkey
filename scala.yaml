- project:
    name: @repo@
    repo: @repo@
    organization: @group@
    type: sbt
    build-results-trigger: ""
    jobs:
      - "{name}-build"
      - "{name}-release"
      - "{name}-pull-request"
      - "{name}-jjb"
