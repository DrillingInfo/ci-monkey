- project:
    name: @repo@
    repo: @repo@
    organization: @group@
    type: cookbook
    build-results-trigger: ""
    jobs:
      - "{name}-build"
      - "{name}-pull-request"
      - "{name}-jjb"
