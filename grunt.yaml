- project:
    name: @repo@
    repo: @repo@
    organization: @group@
    type: javascript
    build-results-trigger: ""
    jobs:
      - "{name}-build"
      - "{name}-pull-request"
      - "{name}-jjb"
